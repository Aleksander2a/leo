{
  "id": "long_thread_compaction",
  "version": "v1",
  "purpose": "Verify a long synthetic thread compacts an immutable prefix, retains recent turns, reduces context, and rejects an incomplete summary.",
  "provider_mode": "offline",
  "fixture_digest": "b455cc4c9e09644eb9b8ad54f2a311d7b66bd2fc3c4e4daca1afb17dd0b4ff87",
  "fixed_clock": "2026-01-02T15:04:05Z",
  "deterministic_id_prefix": "eval-long-thread",
  "execution_variant": "long_thread_compaction",
  "budget": {"max_model_calls": 1, "max_tool_calls": 0, "max_elapsed_seconds": 5},
  "inputs": {"message_count": 60, "recent_window": 12},
  "expected_hard_invariants": ["long_thread_compaction_triggered", "compaction_sources_complete", "compaction_recent_window_preserved", "compaction_reduces_context", "compaction_incomplete_summary_rejected"],
  "expected_quality_metrics": ["compaction_count", "long_thread_message_count", "compacted_message_count", "recent_message_count", "compaction_input_tokens", "compaction_retained_tokens", "compaction_token_reduction_ratio"],
  "allowed_nondeterminism": []
}
